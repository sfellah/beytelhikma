# سجلّ الإصدارات

> هذا الملف هو **مصدر** ملاحظات الإصدار: صفحة `/releases` في الموقع ونصّ إصدار
> GitHub كلاهما مشتقّ منه. عناوين الأقسام مفاتيح ثابتة — `added` و`changed`
> و`fixed` و`removed` و`security` — تُترجم عند العرض. ويجب أن يرد كلّ إصدار في
> الملفّات الثلاثة بالتاريخ نفسه، وإلّا فشل البناء.

## [0.4.0] — 2026-08-02

### added
- صار تقليب الصفحة يتم بثلاث طرق: النقر على الثلث الذي يبدأ فيه السطر للرجوع، والثلث الذي ينتهي فيه للتقدّم، والسحب بالإصبع أو بالقلم في اتجاه سريان النص.
- صار تطبيق أندرويد يحمل فهرس الكتب داخل ملفّ الـ APK ويقترح تحديثه عند بدء التشغيل — لم يعد التشغيل الأول بحاجة إلى سلك توصيل.
- صار أندرويد حاضرًا في صفحة التنزيل وفي الصفحة الرئيسية، إلى جانب ويندوز ولينكس، مع تنبيه بأنّ ملفّ الـ APK يحمل توقيع تصحيح الأخطاء ريثما يوجد مفتاح ناشر.

### removed
- اختفى وضع القراءة بالتمرير المتصل: المتن مُرقَّم صفحة صفحة من أوله إلى آخره — الترويسة السفلية المطبوعة، وكسر الشريط، ومرساة التعليقات، ورابط `?page=` — والتمرير المتصل يفرض التخلي عن هذه المعالم واحدًا تلو الآخر. تبقى القراءة على وضع واحد: الصفحة المطبوعة.

## [0.3.1] — 2026-08-01

### fixed
- كان القارئ يشير إلى الجهة الخطأ في الواجهة الإنجليزية: زرّ الخروج وسهما التنقّل بين الصفحات يدلّان على عكس ما يفعلان. صارا يتبعان اتجاه الواجهة.
- كان شريط تقدّم القراءة يمتلئ من الحافة الخطأ في الإنجليزية: المقبض يمضي يمينًا واللون يرتفع من اليسار.
- كانت أسهم لوحة المفاتيح تقلّب الصفحات عكسيًّا في الإنجليزية، وبطاقة الاختصارات تذكر المفتاح الخطأ.
- صار نصّ الكتب يحمل اتجاهه بنفسه: تبقى الصفحة العربية من اليمين إلى اليسار تحت واجهة إنجليزية، بضبطها وعناوين فصولها وفهرسها ونتائج البحث فيها.
- كانت حركة تقليب الصفحة وسهم فهرس صفحة الكتاب موجَّهين للعربية وحدها.

## [0.3.0] — 2026-08-01

### added
- الإصدار العلني الأول، لويندوز ولينكس.
- فهرس من ٨٥٦٨ نشرة يُثبَّت مع التطبيق: التصفّح والبحث والقراءة لا تحتاج إلى اتصال.
- تنزيل الكتب قابل للاستئناف، ويُتحقَّق من بصمة SHA-256 قبل التثبيت.
- قارئ بوضعين — صفحة مطبوعة وسياق متّصل — مع فهرس المحتويات والبحث داخل الكتاب واختصارات لوحة المفاتيح.
- تعليقات: علامات مرجعية، وأربع درجات للتظليل، وملاحظات، مثبَّتة على النصّ لا على رقم الصفحة.
- ثلاث سمات — ورق، وأبيض، وليل — تسري على التطبيق كلّه.
- واجهة بالعربية والإنجليزية، مع اختيار خطّ الواجهة وخطّ القراءة.
- شاشات للاستكشاف والمجموعات والبحث العابر للمكتبة وإدارة التنزيلات.

### changed
- يُحدِّث الفهرس نفسه من مصدر التوزيع: التحديث يُعرَض ولا يُفرَض، ويُحفَظ الرفض إصدارًا بإصدار.
